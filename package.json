{
  "name": "binlab",
  "version": "1.0.0",
  "description": "Browser-based parametric generator for 3D-printable storage bins and drawer organizers",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test test/geometry.test.js test/layout.test.js",
    "build": "node scripts/build.js",
    "check": "npm test && npm run build"
  },
  "devDependencies": {
    "three": "0.128.0"
  }
}
